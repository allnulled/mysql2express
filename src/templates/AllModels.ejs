const fs = require("fs");
const path = require("path");
const models = fs.readdirSync(__dirname + "/models")
	.filter(file => file !== "Model.js")
	.map(file => path.resolve(__dirname, "models", file))
	.map(file => require(file));
module.exports = models;